<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.loopdeck">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        tools:replace="android:icon,android:allowBackup,android:roundIcon">
        <activity android:name=".ui.changepassword.ChangePasswordActivity"></activity>
        <activity android:name=".ui.profilescreen.ProfileActivity" />
        <activity android:name=".ui.collection.playlist.PlaylistActivity" />
        <activity android:name=".editor.PlayActivity" />
        <activity android:name=".ui.onboardingscreen.OnBoardingScreenActivity" />
        <activity android:name=".ui.login.WebviewLoginActivity" />
        <activity android:name=".gallery.view.PickerActivity" />
        <activity android:name=".ui.collection.CollectionActivity" />
        <activity android:name=".ui.googledrive.GoogleDriveActivity" />
        <activity android:name=".editor.PreviewPhotoActivity" />
        <activity android:name=".editor.PreviewVideoActivity" />
        <activity android:name=".ui.main.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".onedrive.ApiExplorer" />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.example.loopdeck.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true"
            tools:replace="android:authorities">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/provider_paths" />
        </provider>

        <uses-library
            android:name="org.apache.http.legacy"
            android:required="false" />

    </application>

</manifest>